generate detailed requirement specifications for a llm powered rust knowedge graph builder. the application will also provide indexation and searching, navigation capabilities across the graph nodes. It will allow users to filter for topics extracted from the knowledge graph. We will need to index different collection of documents stored in file folders, google drive (later phase). tech stack lancedb, rust, python for interfacing with rust libraries, react frontend with material ui, vite.js. jwt authentication using google id for users.
